alias ping="ping -c4"
alias lyncpidgin="export NSS_SSL_CBC_RANDOM_IV=0 && pidgin  &"
alias vi='/usr/bin/vim'

tailf() {
    tail -f "$*" | ccze -A
}

chmoddir() {
    find "$*" -type d -exec chmod 755 {} +
}

chmodfile() {
    find "$*" -type f -exec chmod 644 {} +
}
